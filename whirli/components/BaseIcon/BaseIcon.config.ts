import { ComponentStyles, ComponentProps, ComponentConfig } from '@whirli-components/@types/components';

import * as PropHelpers from '@whirli-components/helpers/props';

// Local icons
// import LocalIcon from './icons/LocalIcon.vue';
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const LocalIcons: Record<string, any> = {
  // LocalIcon,
};

const styles: ComponentStyles = {};

const props: ComponentProps = {};

const Config: ComponentConfig = { styles, props };

export default Config;
